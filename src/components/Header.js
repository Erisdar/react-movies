import React from 'react';
import BrandTitle from './BrandTitle';
import '../styles/header.css';

export default () => (
    <div id="header" className="justify-content-center">
        <BrandTitle />
    </div>
);